function FilterBtn({title}) {
    return (
        <button className="p-4 rounded-2xl font-medium border border-gray-100 shadow-md hover:shadow-xl transition transform duration-150 active:scale-95">{title}</button>
    )
}

export default FilterBtn
